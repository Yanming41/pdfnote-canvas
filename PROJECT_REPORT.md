# PDF Canvas AI Annotation 项目汇报总结

## 1. 项目一句话概述

这个项目的目标不是再做一个普通 PDF 阅读器，而是做一个“面向学习和研究的 AI 批注画布”：用户像在 Edge 浏览 PDF 一样直接看完整页面，用鼠标选中词、公式、句子或段落，然后围绕这个选区和 AI 进行多轮对话；所有对话最终沉淀为一篇结构化阅读笔记。

核心思路是：**PDF 原文不是聊天附件，而是一个可交互的知识画布；选中的文本块是对话的根节点。**

## 2. 我最初的问题意识

传统 PDF 学习流程有几个断点：

1. PDF 阅读和 AI 提问是割裂的。通常需要复制一段文字到 ChatGPT，再手动描述上下文。
2. AI 回答经常脱离用户真正选中的局部内容，变成对整篇文章或整个主题的泛泛解释。
3. 笔记、提问、AI 回答散落在聊天记录里，最后很难复盘“我当时到底对 PDF 的哪一处产生过疑问”。
4. 普通高亮只保存“我觉得重要”，但不保存“我为什么不懂、我问了什么、AI 怎么解释、我后续又追问了什么”。

所以这个项目想解决的不是“如何显示 PDF”，而是：

> 如何把阅读中的局部困惑，变成可追踪、可复盘、可导出的结构化学习记录。

## 3. 交互创新点

### 3.1 画布式 PDF 阅读，而不是文本抽取式阅读

一开始尝试过终端文本式 PDF 阅读，把 PDF 抽成段落后在 TUI 里浏览。但这个方式不符合真实阅读习惯，因为 PDF 的视觉布局、页码、公式位置、段落关系都会丢失。

最终改成画布式实现：

- 后端用 PyMuPDF 把每页渲染成 PNG。
- 前端按页面原样纵向展示，接近 Edge / 浏览器 PDF 阅读体验。
- 页面图片上方叠加透明文字层，用于鼠标选词和选段。
- 批注高亮层覆盖在页面上，保留选区位置。

这样用户不是在读“抽出来的一段段文本”，而是在读原 PDF 页面本身。

### 3.2 鼠标选区直接成为 AI 对话入口

用户不需要复制粘贴，也不需要手动说明“我问的是哪一段”。

交互流程是：

1. 在 PDF 页面上拖选一段文字。
2. 右侧面板显示当前 Selection。
3. 输入问题。
4. AI 回答被保存为该选区的批注。
5. PDF 页面上对应区域出现高亮。

这个设计把“选区”变成了语义锚点，而不只是视觉高亮。

### 3.3 选中文本块是根节点，而不是一问一条平铺记录

这是项目里最重要的结构设计。

不是这样：

```text
Annotation 1: 选区 A + 问题 1 + 回答 1
Annotation 2: 选区 A + 问题 2 + 回答 2
Annotation 3: 选区 B + 问题 1 + 回答 1
```

而是这样：

```text
Text Block A
  ├─ Q1
  ├─ A1
  ├─ Q2
  └─ A2

Text Block B
  ├─ Q1
  └─ A1
```

也就是说：**一块被选中的 PDF 文本，是后续多轮对话的根节点。**

这个设计更符合学习过程：一个概念、一句话、一个公式，往往不会只问一次；用户可能会连续追问“这是什么意思”“为什么这样推导”“能不能举例”“和前面定义有什么关系”。这些都应该挂在同一个文本块下面。

### 3.4 右侧面板变成“阅读思维链”

右侧不是普通聊天窗口，而是按文本块组织的阅读记录：

- 每个文本块显示原文引用。
- 文本块下面显示该块的多轮 Q/A。
- 点击已有文本块可以继续追问。
- 点击文本块可以跳回 PDF 页面对应位置。

最后右侧面板自然形成一篇“我读这篇 PDF 时的思考路径”。

## 4. AI 上下文设计创新点

### 4.1 选区优先，整篇 PDF 只做辅助上下文

项目中出现过一个关键问题：AI 拿到整篇 PDF 后，容易回答成“整篇文章总结”，而不是解释用户选择的那一小段。

因此 prompt 被改成了强约束结构：

- `SELECTED_TEXT` 是回答的根对象。
- `PAGE_CONTEXT` 只用于解释选区附近的含义。
- `FULL_PDF_REFERENCE` 只作为背景参考。
- 明确禁止在用户没有要求时总结整篇 PDF。
- 如果使用整篇上下文，必须回连到选中文本。

核心原则是：

> AI 可以看全文，但回答必须锚定选区。

### 4.2 继续追问时自动恢复原始选区

多轮对话还有一个隐藏问题：用户点“继续追问”时，前端只传了 `block_id + question`，如果后端不处理，AI prompt 里就会丢失原始选中的文本。

现在的处理方式是：

1. 前端传 `block_id`。
2. 后端根据 `block_id` 读取原文本块。
3. 后端自动补回：
   - 原始 selected_text
   - page
   - surrounding_text
   - rects
   - 历史对话 messages
4. 再构造 prompt。

这样每次追问都仍然围绕同一个文本根节点。

### 4.3 整份 PDF 作为 page-delimited reference

每次问答 prompt 不只传当前页，还包含整份 PDF 的按页文本抽取：

```text
--- Page 1 ---
...
--- Page 2 ---
...
```

这样 AI 可以理解术语、定义、前后文关系。但为了避免 prompt 过长，目前设置了字符预算，超出会截断并标记。

## 5. 数据结构创新点

当前后端从扁平 annotation 进化成两个核心表：

```text
canvas_blocks
  id
  pdf_id
  page
  selected_text
  surrounding_text
  rects_json
  created_at

canvas_messages
  id
  block_id
  role
  content
  created_at
```

这个结构表达的是：

- `canvas_blocks`：阅读中的“问题对象”，也就是 PDF 上被选中的文本块。
- `canvas_messages`：围绕这个文本块发生的多轮思考和解释。

这样导出 Markdown 时可以天然生成：

```md
## Page 1

### Text Block #1
> selected text...

Q: ...
A: ...

Q: ...
A: ...
```

这比传统聊天记录更适合作为学习笔记。

## 6. 技术实现要点

### 6.1 后端

- Python 本地服务：`ThreadingHTTPServer`
- PDF 渲染和文本抽取：`PyMuPDF`
- 数据持久化：SQLite，位于 `~/.pdfnote/pdfnote.sqlite3`
- AI 接入：通过 `PDFNOTE_AI_CMD` 外部命令接口
- 当前 AI wrapper：`ai-claude.ps1`，调用 `claude -p`

### 6.2 前端

- 原生 HTML/CSS/JS，无复杂框架依赖。
- PDF 页面以图片形式展示。
- 透明 text layer 负责鼠标选择。
- highlight layer 负责绘制批注高亮。
- side panel 负责显示 selection、text block、thread。

### 6.3 AI 调用链

```text
鼠标选区 + 问题
  -> POST /api/chat
  -> 后端读取 selected block / history / full PDF text
  -> build_prompt()
  -> PDFNOTE_AI_CMD
  -> Claude CLI
  -> 回答写入 canvas_messages
  -> 前端刷新文本块对话链
```

## 7. 和普通方案的区别

### 7.1 相比普通 PDF 高亮

普通高亮只保存“这段重要”。本项目保存的是：

- 这段在哪里
- 原文是什么
- 我问了什么
- AI 怎么解释
- 我后来如何追问
- 最后能导出成结构化笔记

### 7.2 相比普通 ChatGPT 问答

普通 ChatGPT 问答很难保留 PDF 空间位置和选区关系。本项目把每次问答绑定到 PDF 坐标和文本块。

### 7.3 相比纯文本 PDF 阅读器

纯文本阅读器容易丢失 PDF 的视觉布局。本项目保留页面画布，同时叠加可交互文本层。

## 8. 当前项目状态

已经实现：

- 打开本地 PDF。
- 按页面完整渲染 PDF。
- 鼠标选词/选段。
- 右侧显示当前选区。
- 基于选区向 AI 提问。
- AI 回答保存为批注。
- 同一个选区支持多轮对话链。
- PDF 页面高亮批注位置。
- Markdown 导出。
- 通过 Claude CLI 接入真实 AI。
- 清空旧历史、修复 AI 偏离选区的问题。

当前运行地址示例：

```text
http://127.0.0.1:8766/
```

项目路径：

```text
C:\Users\13116\pdfnote-canvas
```

## 9. 后续可以继续优化的方向

### 9.1 选区与高亮更稳定

当前选区依赖透明文本层和浏览器 selection。后续可以进一步优化：

- 多行选区 rect 合并。
- 选区跨页处理。
- 高亮颜色分类。
- 鼠标悬浮高亮时显示对应对话卡片。

### 9.2 更强的笔记组织

可以加入：

- 标签：定义、疑问、例子、公式、待复习。
- 按页码/标签/时间过滤。
- 自动生成整篇 PDF 的学习摘要。
- 自动列出“我还没弄懂的问题”。

### 9.3 更强的 AI 上下文检索

现在是把整份 PDF 文本按预算传入。后续可以改成：

- 按选区附近内容做局部检索。
- 建立 PDF chunk index。
- 只传最相关页和段落。
- 支持多个 PDF 之间的交叉引用。

### 9.4 和个人知识库连接

导出的 Markdown 可以进一步接入 Obsidian：

- 每个 Text Block 变成一个引用块。
- 每条 Q/A 变成可搜索知识记录。
- 和课程、概念、公式页做双链。

## 10. 汇报时可以强调的核心观点

这个项目的核心创新不在“PDF 能显示”或“AI 能回答”，而在于重新定义了 PDF 阅读中的知识结构：

> 选中文本块是学习过程中的最小知识锚点；围绕它发生的多轮提问、解释和追问，才是真正有价值的阅读笔记。

它把 PDF 阅读、AI 辅助理解、批注、高亮和笔记导出合并成一个连续工作流，使用户最后得到的不是散乱聊天记录，而是一篇按原文位置组织的个人理解文档。
